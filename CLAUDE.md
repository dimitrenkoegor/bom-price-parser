# BOM Price Parser — карта проекта

Автопоиск цен на ЭКБ: письмо с BOM → парсер → API дистрибьюторов → ответ письмом.
Этот файл — только маршрутизатор. Детали читать в целевых файлах, а не здесь.

## Куда смотреть

| Нужно | Файл |
|---|---|
| Правила подбора цен (регламент закупок) | `РЕГЛАМЕНТ_для_ИИ.md` |
| CLI-контракт парсера, флаги, окружение | `AGENTS.md` |
| Поиск цен: 4 API, маски MPN, бренды, LLM-контур | `price_parser.py` |
| Почта: приём, автоответ, санити-чеки | `mail_monitor.py` |
| Браузерный каскад по BOM-инструментам площадок | `browser_bom.py` |
| Процедура браузерной ноги каскада | `.claude/skills/bom-browser-cascade/SKILL.md` |
| Конвертер выгрузки oemsecrets BoM Tool | `bomtool_to_final.py` |
| Тесты (32 шт.) | `tests/test_price_parser.py` |
| Ключи API и флаги поведения | `.env` (не в git) |

## Команды

```bash
python price_parser.py --input "запрос.xlsx" --output "итог.xlsx" --yes
python price_parser.py --once <PN> <QTY> --manufacturer <бренд> --yes
PYTHONPATH=. python -m unittest tests.test_price_parser
```

Монитор: `START_MAIL.bat` / `STOP_MAIL.bat`; автозапуск: `INSTALL_AUTOSTART.bat` / `UNINSTALL_AUTOSTART.bat`.

## Что важно знать

- Запускать монитор **одним** способом (руками ИЛИ автозапуск) — два экземпляра дерутся за ящик.
- `mail-state.json` — отметки обработанных писем. **Не удалять**: иначе повторные ответы клиентам.
- Цена берётся из API дистрибьютора **или** из BOM-инструмента площадки через
  браузерный каскад (`browser_bom.py`, скилл `bom-browser-cascade`). Браузерные
  строки красятся сиреневым и считаются в сноске отдельно от LLM-овых (персиковых).
  LLM-контур предлагает написание артикула, но строка заполняется лишь после
  подтверждения через API — цену модель дать не может.
- `HOLD_ON_RFQ` в `.env`: `false` — полный автомат; `true` — придержать ответ при остатке RFQ.
- Позиции, добытые LLM-контуром, красятся персиковым и считаются в сноске итогового xlsx.

## Открытые задачи

- Ждём ключ **Arrow API** (заявка 16.07) → 5-й источник, без дневных квот.
- Нужен ключ **oemsecrets** (`OEMSECRETS_API_KEY`) → добивочный фолбэк, 10 позиций/день.

## Рабочие привычки

Одна задача — один чат, потом `/clear`. Этот файл грузится автоматически, контекст не теряется.
